[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = src/tests
env =
    ENVIRONMENT=testing
addopts =
    --cov=src
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=60
